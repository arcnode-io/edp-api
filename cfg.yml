local:
  log_level: DEBUG
  host: '127.0.0.1'
  port: 8000
  e2e: false
  reload: true
  # edp-module-assemblies publishes manifest.yaml to S3; edp-api fetches at startup.
  # Local dev points at LocalStack via S3_ENDPOINT_URL.
  manifest_url: 's3://arcnode-artifacts/manifest.yaml'
beta:
  log_level: INFO
  host: '0.0.0.0'
  port: 8000
  e2e: true
  reload: false
  manifest_url: 's3://arcnode-artifacts/manifest.yaml'
